/*
incrementer function takes an argument num, and then returns a result after increasing the value by one.
*/

// The global variable
var fixedValue = 4;

function incrementer (num) {
  return num + 1;
}

console.log(incrementer(fixedValue));